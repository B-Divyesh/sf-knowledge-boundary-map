[
  {
    "id": "demo-sandbox",
    "claim": "Try it with sample data without touching your real map",
    "where": "landing action and demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context; load the sample, inspect the demo storage namespace, then start for real and confirm the real map is empty"
  },
  {
    "id": "local-only",
    "claim": "Your map stays in this browser unless you export it",
    "where": "landing facts, privacy notice, README",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "fresh browser context; create and rehearse a sample claim while recording every request origin"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after your first visit",
    "where": "landing facts, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "a dedicated browser context visits the production preview, waits for service-worker control, goes offline, and reloads"
  },
  {
    "id": "csv-export",
    "claim": "Exports your map as CSV",
    "where": "Export dialog, README",
    "test": "npm run test:e2e -- --grep @claim:csv-export",
    "sandbox": "fresh browser context; open the shipped sample and assert the downloaded CSV header and three sample rows"
  },
  {
    "id": "studio-price-checkout",
    "claim": "Studio is a $12 USD one-time purchase",
    "where": "landing facts, upgrade page, terms",
    "test": "npm run test:billing",
    "sandbox": "production billing catalog contains this slug at 1200 USD cents and its checkout endpoint returns a hosted-checkout redirect"
  }
]
